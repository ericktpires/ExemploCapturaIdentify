pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven {
            url 'https://identify-maven.stoneage.com.br'
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url 'https://identify-maven.stoneage.com.br'
        }
    }
}
rootProject.name = "IdentifyDemo2"
include ':app'
